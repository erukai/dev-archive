> _committed: 27/11/2025_

_(all examples can be found in the files in this directory.)_

---

database
- an organized digital location to store data
- data can be stored in a structured or unstructured way
- a database is managed by a system called **"Database Management System"** *(DBMS)*
- two common database categories are Relational (SQL) and Non-Relational (NoSQL)

Relational Database (SQL)
- data is structured
- data is stored in a table with rows and columns
- tables are linked using predefined relationships (primary and foreign keys)
- examples: `MySQL`, `PostgreSQL`, `Oracle`

Non-Relational Database (NoSQL)
- data is _not really_ "unstructured", bur rather _"flexible"_
- data is stored using models like key-value pairs, JSON-like documents or graphs
- examples: `MongoDB`, `Redis`, `Cassandra`

_(Trivia: "SQL", pronounced like "sequel", is an acronym for "Structured Query Language")_

`.json`
- a plain text file that stores structured data in the **JavaScript Object Notation** *(JSON)* format
- although built in JavaScript, the `.json` format itself is language-independent
- `.json` is a data-interchange format and is not a database, but it can still be used to store simple data, which is why it is used frequently by many programming languages
